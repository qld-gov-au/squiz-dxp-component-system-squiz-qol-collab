// import CSS
import "@styles/main.scss";
// Dynamically load in client side component JS
//const components = import.meta.glob('@scripts/components/**/*.js', { eager: true });
//for (const path in components) {}
